import HeroCard from "./ui/hero/heroCard";

export default function HomePage() {
  return (
    <main>
      <section>
        <HeroCard />
      </section>
      <section>
       what we do?
      </section>
      <section>
        how it works
      </section>
      <section>
        why choose us?
      </section>
    </main>
  )
}
